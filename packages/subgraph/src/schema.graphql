type Balance @entity {
  id: ID!
  token: Bytes!
  quantity: BigInt!
  price: BigInt!
  createdAt: BigInt!
}

type Portfolio @entity {
  id: ID!
  address: Bytes!
  name: String!
  tokens: [Bytes!]!
  createdAt: BigInt!
}
